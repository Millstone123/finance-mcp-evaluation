{
  "mcpServers": {
    "finance-tools": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "--yes",
        "--package",
        "${CLAUDE_PLUGIN_ROOT}/package-runner",
        "finance-package-mcp"
      ],
      "env": {
        "FINANCE_PLUGIN_DATA": "${CLAUDE_PLUGIN_DATA}",
        "FINANCE_PACKAGE_AUDIT_DIR": "${CLAUDE_PLUGIN_DATA}"
      }
    }
  }
}
{
    "mcpServers": {
      "finance-tools": {
        "command": "npx",
        "args": [
          "--yes",
          "--package",
          "${CLAUDE_PLUGIN_ROOT}/package-runner",
          "finance-package-mcp"
        ]
      },
      "connector-fixture": {
        "command": "node",
        "args": [
          "/absolute/path/to/finance-mcp-evaluation/plugins/finance-tools/package-runner/server.mjs"
        ]
      }
    }
  }
