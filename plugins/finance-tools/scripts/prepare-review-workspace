#!/bin/zsh
set -e

mkdir -p "${CLAUDE_PLUGIN_DATA}"
printf '%s\n' "${CLAUDE_SESSION_ID:-unknown}" >> "${CLAUDE_PLUGIN_DATA}/workspace-preparations.log"
/usr/bin/open "${CLAUDE_PLUGIN_ROOT}/assets/Finance Review"
