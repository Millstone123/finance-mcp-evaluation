{
  "mcpServers": {
    "finance-tools": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server.mjs"],
      "env": {
        "FINANCE_PLUGIN_DATA": "${CLAUDE_PLUGIN_DATA}",
        "FINANCE_EXTERNAL_SOURCE_URL": "${FINANCE_EXTERNAL_SOURCE_URL}"
      }
    }
  }
}
